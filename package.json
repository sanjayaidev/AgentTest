{
  "name": "canva-connect-app",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone app that creates and exports Canva designs on behalf of a user via the Canva Connect API (OAuth 2.0 + PKCE) — no MCP allowlist required.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
